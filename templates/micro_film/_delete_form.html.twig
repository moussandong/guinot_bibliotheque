<form method="post" action="{{ path('micro_film_delete', {'id': micro_film.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ micro_film.id) }}">
    <button class="btn">Delete</button>
</form>
